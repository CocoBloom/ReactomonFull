import styled from 'styled-components';


export default styled.div`
    /* background-color: ${props => props.primary ? 'lightgreen' : 'lightcoral'} */
    background: ${props => props.theme.primary};
    `